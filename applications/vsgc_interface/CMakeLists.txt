SET(APPLICATION_NAME vsgc_interface)

INCLUDE_DIRECTORIES(
    ${CMAKE_SOURCE_DIR}/include/
    ${Vulkan_INCLUDE_DIR}
)

SET(SOURCES
    vsgc_interface.c
)


ADD_EXECUTABLE(${APPLICATION_NAME} ${HEADERS} ${SOURCES})

TARGET_LINK_LIBRARIES(${APPLICATION_NAME} vsg ${Vulkan_LIBRARY})
